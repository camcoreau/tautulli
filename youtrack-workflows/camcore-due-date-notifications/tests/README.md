# Tests

`test-overdue-notifications.js` is a repository-only regression test. It verifies
that the CamCore workflow preserves JetBrains' scheduling and recipient-selection
behaviour while locking the approved CamCore subject, HTML identity, links and
HTML-escaping safeguards.

This directory must not be included in the ZIP uploaded to YouTrack.
